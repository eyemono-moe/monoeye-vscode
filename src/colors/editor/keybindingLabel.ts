import type { EditorColorGenerator } from "../../types/editorColors";

export const keybindingLabel: EditorColorGenerator = (palette) => {
  return {
    keybindingLabel: {
      // background: "new Color(new RGBA(128, 128, 128, 0.17))",
      // border: "new Color(new RGBA(51, 51, 51, 0.6))",
      // bottomBorder: "new Color(new RGBA(68, 68, 68, 0.6))",
      // foreground: 'Color.fromHex("#CCCCCC")',
    },
  };
};
