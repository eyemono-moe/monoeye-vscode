export type HexColor = `#${string}`;

export type ColorPalette = {
  bgLight: HexColor;
  bgMain: HexColor;
  bgDark: HexColor;
  bgDarker: HexColor;
  fgMain: HexColor;
  fgSub: HexColor;
  fgShadow: HexColor;
  red: HexColor;
  orange: HexColor;
  yellow: HexColor;
  green: HexColor;
  cyan: HexColor;
  blue: HexColor;
  purple: HexColor;
};
