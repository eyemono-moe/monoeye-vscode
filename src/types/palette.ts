export type HexColor = `#${string}`;

export type ColorPalette = {
  bgLight: HexColor;
  bgMain: HexColor;
  bgDark: HexColor;
  bgDarker: HexColor;
  fgMain: HexColor;
  fgSub: HexColor;
  fgShadow: HexColor;
  orange: HexColor;
  red: HexColor;
  yellow: HexColor;
  green: HexColor;
  cyan: HexColor;
  blue: HexColor;
  purple: HexColor;
};
